<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>


      <nav class="nav-bar">

        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/homepage">Home</a> |
        <a href="/profile">Profile</a> |
        <a href="/post" class="nav-link">Dashboard</a>
        <button class="no-button nav-link" id="logout">Logout</button>
        {{else}}
        <a href="/login" class="nav-link">Login</a>
        {{/if}}

               <div class="nav-title"><h1>The Tech Blog</h1></div>
      </nav>
    </header>
    <main class="container container-fluid mt-5 main-section">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
